# Excel 散点图桌面应用 — 项目开发规范（完整版）

> **文档性质**：项目开发规范（架构冻结版 v3.0 固化稿 + UI 设计规范补充）
> **文档版本**：v3.1（含 2026-08-20 客户确认的 6 项 UI 决策）
> **创建日期**：2026-08-17 ｜ **最近更新**：2026-08-20
> **文档状态**：✅ 已冻结（进入 Phase 1 编码阶段；新增内容见第 14 章，并以第 14 章为准）
> **适用读者**：项目经理、架构师、AI 开发工程师、测试工程师
> **生效日期**：自本文件签署之日起
> **冻结依据**：客户需求澄清会议（2026-08-17）6 项核心决策 + 客户 UI 设计稿确认（2026-08-20）6 项 UI 决策

---

## 目录

- [第 0 章 文档说明与冻结含义](#第-0-章-文档说明与冻结含义)
- [第 1 章 项目核心目标](#第-1-章-项目核心目标)
- [第 2 章 关键架构决策速查表（6 项确认）](#第-2-章-关键架构决策速查表6-项确认)
- [第 3 章 数据流架构（闭环模型）](#第-3-章-数据流架构闭环模型)
- [第 4 章 技术栈与工程结构（补充建议）](#第-4-章-技术栈与工程结构补充建议)
- [第 5 章 最终数据库表结构（冻结版）](#第-5-章-最终数据库表结构冻结版)
- [第 6 章 UI 交互架构（冻结版）](#第-6-章-ui-交互架构冻结版)
- [第 7 章 导出覆盖策略与取消机制（方案 A 细化）](#第-7-章-导出覆盖策略与取消机制方案-a-细化)
- [第 8 章 性能阈值与验收标准（修正版）](#第-8-章-性能阈值与验收标准修正版)
- [第 9 章 开发计划调整（基于冻结架构）](#第-9-章-开发计划调整基于冻结架构)
- [第 10 章 变更管理（ACR 流程）](#第-10-章-变更管理acr-流程)
- [第 11 章 附录 A：16 种 Excel 标准色（UI 调色板硬编码）](#第-11-章-附录-a16-种-excel-标准色ui-调色板硬编码)
- [第 12 章 附录 B：代码强制校验规则（Pydantic 风格）](#第-12-章-附录-b代码强制校验规则pydantic-风格)
- [第 13 章 签署确认](#第-13-章-签署确认)
- [第 14 章 UI 设计规范（冻结版，2026-08-20 确认）](#第-14-章-ui-设计规范冻结版2026-08-20-确认)
- [附录 C：文档变更记录](#附录-c文档变更记录)

---

## 第 0 章 文档说明与冻结含义

本文件为 **《03-开发计划书.md》** 的架构级补充与修正，是对该计划中模糊部分的**最终解释与固化**，同时作为 Phase 1~Phase 8 编码阶段的**唯一开发依据**。

### 0.1 冻结含义

- 本文件中的所有架构决策均已锁定，作为 Phase 1~Phase 8 编码的**唯一依据**。
- 后续任何需求变更，均需提交《架构变更请求（ACR）》并经客户签字后方可实施（流程见第 10 章）。
- 开发过程中若发现本文件与《03-开发计划书.md》冲突，**以本文件为准**。

### 0.2 文档使用指引

| 使用场景 | 应阅读章节 |
| :--- | :--- |
| 快速了解项目定位 | 第 1、2 章 |
| 开始写代码前的全局认知 | 第 2、3、4 章 |
| 实现数据模型（Week 1） | 第 5 章 |
| 实现 UI 界面（Week 4~5） | 第 6 章 |
| 实现 UI 新增功能（多选/缩放/导出图片/拖拽/最近项目） | 第 6.4、第 14 章 |
| 实现导入/导出/覆盖（Week 2~3、6） | 第 3、7 章 |
| 验收与测试 | 第 8 章 |
| 开发排期 | 第 9 章 |
| 数据校验与调色板 | 第 11、12 章 |

---

## 第 1 章 项目核心目标

### 1.1 目标修正表（客户决策）

| 维度 | 原计划描述 | 冻结后修正（客户决策） |
| :--- | :--- | :--- |
| **应用定位** | Excel 文件的"编辑器" | **Excel 文件的"生成器/转换器"**：用户导入原始数据 → 应用生成全新的、独立的 `.xlsx` 产出文件（含 raw data + 原生散点图）。 |
| **数据来源** | 直接引用用户原始 Excel | 原始 Excel 仅作为**一次性数据源**，解析后即与系统脱离关系（不存路径、不存引用）。 |
| **数据存储** | 存原始文件路径 | **仅存储导出文件的绝对路径**，数据库绝不存储任何原始数值（浮点数/字符串）。 |
| **图表编辑** | 在原文件上"嵌入"图表 | **每次编辑均为"重新生成新文件"**，覆盖原导出文件（方案 A），形成"导入→生成→编辑→覆盖"闭环。 |
| **点数限制** | 单图最大 1000 点 | **废除点数上限**。用户通过指定起始行/结束行控制数据量。预览端自动降采样（5000 行阈值），导出端全量写入。 |

### 1.2 一句话概括

> 用户导入原始 Excel → 应用读取指定行列切片 → 生成新的 `.xlsx` 文件（含 `raw data` 页 + `Chart View` 页原生散点图）→ 数据库仅记录导出文件路径与行列范围等元数据 → 后续编辑通过覆盖导出文件实现。

---

## 第 2 章 关键架构决策速查表（6 项确认）

以下为客户确认的 6 项核心决策及其对应的**强制性技术规范（不可变更）**：

| 序号 | 决策维度 | 客户选择 | 强制性技术规范（不可变更） |
| :--- | :--- | :--- | :--- |
| **1** | 行选取策略 | 用户指定起始行/结束行；缺省结束行则仅取一行；点数不设限制。 | **UI 组件**：`QSpinBox` 起始行（默认 1）+ `QSpinBox` 结束行（默认最大行数，可置空）。<br>**采样算法**：`df.iloc[start_row:end_row]`（直接切片）。<br>**⚠️ 预览降采样契约**：若切片后数据 > 5000 行，ECharts 端仅展示前 5000 点（前端静默降采样），**导出时仍写入用户指定的全部行数**。 |
| **2** | 导出文件结构 | 复制用户数据，重新创建新 Excel：<br>Sheet1 命名为 `raw data`（原始数据切片）；<br>Sheet2 插入图表。 | **导出流程**：<br>1. 内存中新建 `Workbook()`。<br>2. 创建 `raw data` Sheet，写入用户选定的行列切片数据。<br>3. 创建第 2 个 Sheet（名称固定为 `Chart View`），在该页使用 `openpyxl.chart.ScatterChart` 生成原生散点图，**绑定 `raw data` 页的对应列**。<br>4. 保存为 `原文件名_Scatter_{时间戳}.xlsx`（用户可自定义）。<br>**禁止**：在原用户文件上做任何修改。 |
| **3** | 列选取策略 | 用户指定起始列/结束列（字母或索引）；缺省结束列则仅取一列；真实数据量可能远小于 1 万列。 | **UI 组件**：`QLineEdit` 起始列（默认 A）+ `QLineEdit` 结束列（可置空）。<br>**解析优化**：`ExcelHandler` 在 `read_only` 模式下，**只解析 `min_col` 到 `max_col` 区间**，禁止全量扫描 1 万列（性能提升 5-10 倍）。<br>**列名解析**：`openpyxl` 的 `iter_rows(min_col, max_col, max_row=1)` 仅读标题行。 |
| **4** | 数据持久化 | **不存原始数据**；只存导出文件路径；文件丢失则弹窗提示用户重定位或取消；UI 展示数据库列表支持 CRUD。 | **⚠️ 强制禁令**：SQLite 中**严禁**存储任何原始数值（浮点数/字符串）。`Project` 表仅存：导出文件绝对路径 + 行列范围索引 + 元数据。<br>**数据库体积**：即使管理 1 万个图表项目，SQLite ≤ 100MB。<br>**UI 展示**：项目列表展示文件路径简写 + 状态标签（✅ 存在 / ❌ 缺失）。 |
| **5** | 进度与取消 | 展示进度条；提供取消按钮；安全终止导入导出。 | **强制接口**：所有耗时类（`ExcelHandler`、`ChartBuilder`、`ExcelExportService`）必须实现 `IStoppable` 接口（含 `self._kill_flag` 标志位）。<br>**检查点机制**：每处理 1000 行（或每写入一个 Sheet）检查一次 `should_cancel()`。<br>**回滚契约**：若捕获 `OperationCancelledException`，必须 ① 删除未写完的临时文件；② 回滚数据库事务；③ 还原原文件（覆盖场景下）。 |
| **6** | 样式降级 | A. 静默降级（不提示用户）。 | **UI 调色板硬编码**：仅提供 16 种 `openpyxl` 标准色（`FF0000`, `0000FF`, `00FF00`, ...），禁用取色器/渐变色控件。<br>**形状选择器**：仅限 `circle`、`diamond`、`square`、`triangle` 四种。<br>**无需适配层**：直接传递标准色值和形状给 `openpyxl`。 |

---

## 第 3 章 数据流架构（闭环模型）

### 3.1 三种核心场景

| 场景 | 用户操作流程 | 系统内部行为（架构强制契约） |
| :--- | :--- | :--- |
| **场景 1：首次导入与导出** | ① 导入 `原始数据.xlsx`<br>② 设定行列范围<br>③ 配置系列（X/Y列、颜色、形状）<br>④ 点击"导出" | ① `ExcelHandler` 流式读取行列切片（内存中）。<br>② 内存中新建 `Workbook()`，创建 `raw data` Sheet 写入切片数据。<br>③ 创建 `Chart View` Sheet，调用 `ChartBuilder` 生成原生散点图。<br>④ 保存为 `原始数据_Scatter_时间戳.xlsx`。<br>⑤ 数据库 `Project` 表插入记录：`source_file_path` = 新文件绝对路径，行列范围、配色等元数据。<br>⑥ **原始文件路径绝不入库**。 |
| **场景 2：再次编辑（文件未移动）** | ① 打开软件<br>② UI 项目列表中双击目标图表项目 | ① 数据库读取到 `source_file_path`。<br>② `openpyxl` 以 `read_only=True` 模式打开该文件，读取 `raw data` Sheet 中用户之前选中的列（仅需 1-2 秒）。<br>③ 数据加载至 ECharts 预览，用户可修改配置。<br>④ 点击"导出" → 覆盖原文件（写前备份 → 写入 → 校验 → 替换）。<br>**（实现了"无需重导原始数据"）** |
| **场景 3：文件被移动或删除** | ① 双击灰色不可用的项目<br>② 弹窗提示"数据文件不存在" | ① 应用启动时后台 `FileProbeService` 异步探活，发现路径无效 → `Project.status = MISSING`。<br>② UI 项目变为灰色，右侧出现 **"删除"按钮**（仅此按钮可用）。<br>③ 用户点击"删除" → 仅执行 `DELETE FROM Project` 级联操作（删除数据库记录），**绝不触碰磁盘文件**。<br>④ 用户也可点击"重新定位" → 弹出文件选择器，选中新文件后校验是否含 `raw data` 页 → 通过后更新路径并激活。 |

### 3.2 架构流程图

```
[用户导入原始 Excel]
        ↓
[ExcelHandler 流式读取行列切片] ←── 用户指定起始行/结束行 + 起始列/结束列
        ↓
[内存数据暂存] (仅缓存切片，不落盘)
        ↓
[用户配置系列/颜色/形状]
        ↓
[ExcelExportService 生成新文件]
    ├── 新建 Workbook()
    ├── Sheet1 = "raw data" (写入切片数据)
    ├── Sheet2 = "Chart View" (生成原生散点图)
    └── 保存为 "原文件名_Scatter_时间戳.xlsx"
        ↓
[Project 表插入记录]
    ├── source_file_path = 新文件绝对路径
    ├── raw_start_row, raw_end_row, raw_start_col, raw_end_col
    └── 系列配置 (颜色/形状)
        ↓
[编辑/覆盖循环]
    ├── 读取 source_file_path 的 raw data 页
    ├── 用户修改配置
    └── 覆盖写入 (写前备份 → 写入 → 校验 → 替换)
        ↓
[文件丢失处理]
    ├── FileProbeService 探活失败
    ├── UI 标记为 MISSING (灰色)
    └── 用户可 "删除" (仅删 DB) 或 "重新定位" (更新路径)
```

---

## 第 4 章 技术栈与工程结构（补充建议）

> ⚠️ 本章为整理时的**补充建议**，不属于客户冻结决策；如与冻结决策冲突，以冻结决策为准。

### 4.1 技术栈选型

| 层级 | 技术选型 | 用途说明 |
| :--- | :--- | :--- |
| 语言 | Python 3.10+ | 主开发语言 |
| GUI 框架 | PySide6 / PyQt6 | 桌面界面（`QSpinBox`、`QLineEdit`、`QThread` 等） |
| Excel 读写 | openpyxl | 流式读取（`read_only`）+ 生成新工作簿 + 原生 `ScatterChart` |
| 图表预览 | ECharts（经 QWebEngineView 嵌入） | 实时散点图预览，前端静默降采样 5000 点 |
| 数据库 | SQLite + SQLAlchemy ORM | 仅存路径与元数据（≤ 100MB） |
| 数据切片 | pandas | `df.iloc[start_row:end_row]` 行切片 |

### 4.2 建议工程目录结构

```
Excel 画图/
├── main.py                          # 程序入口
├── requirements.txt                 # 依赖清单
├── README.md                        # 项目说明（文档索引）
├── 项目开发规范.md                  # 本文件（唯一开发依据）
├── src/
│   ├── __init__.py
│   ├── core/
│   │   ├── __init__.py
│   │   ├── database.py              # SQLAlchemy engine / session / Base
│   │   ├── exceptions.py            # OperationCancelledException / OperationFailedException
│   │   └── stoppable.py             # IStoppable 接口定义
│   ├── models/
│   │   ├── __init__.py
│   │   ├── project.py               # Project ORM 模型
│   │   ├── series.py                # Series ORM 模型
│   │   ├── theme.py                 # Theme ORM 模型
│   │   └── validators.py            # 校验规则（第 12 章附录 B）
│   ├── services/
│   │   ├── __init__.py
│   │   ├── excel_handler.py         # ExcelHandler（列区间流式读取）
│   │   ├── chart_builder.py         # ChartBuilder（openpyxl ScatterChart）
│   │   ├── excel_export_service.py  # ExcelExportService（生成新文件 + 覆盖策略）
│   │   └── file_probe_service.py    # FileProbeService（启动异步探活）
│   └── ui/
│       ├── __init__.py
│       ├── main_window.py           # 主窗口
│       ├── widgets/                 # ThemeTreeWidget / ChartListView / ChartPreviewWidget
│       ├── views/                   # ImportView / ConfigView
│       └── dialogs/                 # ExportDialog 等
└── tests/                           # 单元测试（pytest）
```

### 4.3 开发通用约定（补充建议）

- **命名规范**：类名 `PascalCase`，函数/变量 `snake_case`，常量 `UPPER_SNAKE_CASE`。
- **耗时类接口**：`ExcelHandler`、`ChartBuilder`、`ExcelExportService` 必须实现 `IStoppable`（见第 7 章），任何循环体每 1000 行调用一次 `should_cancel()`。
- **异常体系**：业务异常统一继承自 `OperationFailedException`；用户取消统一抛 `OperationCancelledException`。
- **数据库访问**：一律通过 SQLAlchemy Session 操作，禁止裸写 SQL（初始化建表除外）。
- **注释语言**：代码注释与文档统一使用中文。

---

## 第 5 章 最终数据库表结构（冻结版）

> **Week 1 必须实现**，为最终 Schema，**严禁擅自增加字段**（特别是存储原始数据的 BLOB/JSON 字段）。

### 5.1 表结构定义

```sql
-- themes 表 (主题分组)
CREATE TABLE themes (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name TEXT NOT NULL UNIQUE,
    description TEXT,
    sort_order INTEGER DEFAULT 0,            -- 新增(2026-08-20 Q5)：主题拖拽排序
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- projects 表 (图表项目主表 - 核心资产)
CREATE TABLE projects (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name TEXT NOT NULL,
    theme_id INTEGER NOT NULL,

    -- 【唯一存储的"数据"】导出文件的绝对路径
    source_file_path TEXT NOT NULL,

    -- 行列切片元数据 (用于从 raw data 页重新读取)
    raw_start_row INTEGER NOT NULL DEFAULT 1,
    raw_end_row INTEGER,                     -- NULL 表示仅取一行
    raw_start_col_letter TEXT NOT NULL,      -- 如 "A"
    raw_end_col_letter TEXT,                 -- NULL 表示仅取一列

    -- 图表元数据
    chart_title TEXT,
    x_axis_label TEXT,
    y_axis_label TEXT,
    x_axis_min REAL,
    x_axis_max REAL,
    y_axis_min REAL,
    y_axis_max REAL,
    show_grid BOOLEAN DEFAULT 1,

    -- 状态与时间戳
    status TEXT DEFAULT 'ACTIVE',            -- 'ACTIVE' | 'MISSING'
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    last_opened_at TIMESTAMP,                 -- 新增(2026-08-20 Q6)：最近打开时间

    FOREIGN KEY (theme_id) REFERENCES themes(id) ON DELETE CASCADE
);

-- series 表 (系列映射)
CREATE TABLE series (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    project_id INTEGER NOT NULL,
    series_name TEXT NOT NULL,               -- 用户自定义系列名称
    x_col_letter TEXT NOT NULL,              -- X轴列字母 (如 "A")
    y_col_letter TEXT NOT NULL,              -- Y轴列字母 (如 "C")
    color_hex TEXT NOT NULL,                 -- 仅限 16 种标准色 (如 #FF0000)
    marker_shape TEXT NOT NULL DEFAULT 'circle',  -- circle/diamond/square/triangle
    marker_size INTEGER DEFAULT 8,
    sort_order INTEGER DEFAULT 0,

    FOREIGN KEY (project_id) REFERENCES projects(id) ON DELETE CASCADE
);

-- 索引
CREATE INDEX idx_projects_theme_id ON projects(theme_id);
CREATE INDEX idx_projects_status ON projects(status);
CREATE INDEX idx_series_project_id ON series(project_id);

-- 【可选·2026-08-20 Q6】若需持久化"最近项目"访问历史，可另建独立记录表（方案 B）。
-- 本规范默认采用方案 A：projects 表 last_opened_at 字段，按降序取前 5 条即可，无需新表。
-- CREATE TABLE recent_projects (
--     id INTEGER PRIMARY KEY AUTOINCREMENT,
--     project_id INTEGER NOT NULL,
--     opened_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
--     FOREIGN KEY (project_id) REFERENCES projects(id) ON DELETE CASCADE
-- );
```

### 5.2 SQLAlchemy ORM 映射（Python）

```python
# src/models/project.py
from sqlalchemy import Column, Integer, String, Float, Boolean, DateTime, ForeignKey
from sqlalchemy.orm import relationship
from datetime import datetime
from src.core.database import Base

class Project(Base):
    __tablename__ = 'projects'

    id = Column(Integer, primary_key=True)
    name = Column(String(200), nullable=False)
    theme_id = Column(Integer, ForeignKey('themes.id', ondelete='CASCADE'), nullable=False)

    source_file_path = Column(String(500), nullable=False)

    raw_start_row = Column(Integer, nullable=False, default=1)
    raw_end_row = Column(Integer, nullable=True)
    raw_start_col_letter = Column(String(5), nullable=False)
    raw_end_col_letter = Column(String(5), nullable=True)

    chart_title = Column(String(200), nullable=True)
    x_axis_label = Column(String(100), nullable=True)
    y_axis_label = Column(String(100), nullable=True)
    x_axis_min = Column(Float, nullable=True)
    x_axis_max = Column(Float, nullable=True)
    y_axis_min = Column(Float, nullable=True)
    y_axis_max = Column(Float, nullable=True)
    show_grid = Column(Boolean, default=True)

    status = Column(String(20), default='ACTIVE')
    created_at = Column(DateTime, default=datetime.now)
    updated_at = Column(DateTime, default=datetime.now, onupdate=datetime.now)
    last_opened_at = Column(DateTime, nullable=True)   # 新增(2026-08-20 Q6)：最近打开时间

    # 关系
    theme = relationship("Theme", back_populates="projects")
    series_list = relationship("Series", back_populates="project", cascade="all, delete-orphan")
```

```python
# src/models/series.py
from sqlalchemy import Column, Integer, String, ForeignKey
from sqlalchemy.orm import relationship
from src.core.database import Base

class Series(Base):
    __tablename__ = 'series'

    id = Column(Integer, primary_key=True)
    project_id = Column(Integer, ForeignKey('projects.id', ondelete='CASCADE'), nullable=False)
    series_name = Column(String(100), nullable=False)
    x_col_letter = Column(String(5), nullable=False)
    y_col_letter = Column(String(5), nullable=False)
    color_hex = Column(String(7), nullable=False)      # 如 "#FF0000"
    marker_shape = Column(String(20), nullable=False, default='circle')
    marker_size = Column(Integer, default=8)
    sort_order = Column(Integer, default=0)

    project = relationship("Project", back_populates="series_list")
```

```python
# src/models/theme.py
from sqlalchemy import Column, Integer, String, DateTime
from sqlalchemy.orm import relationship
from datetime import datetime
from src.core.database import Base

class Theme(Base):
    __tablename__ = 'themes'

    id = Column(Integer, primary_key=True)
    name = Column(String(200), nullable=False, unique=True)
    description = Column(String(500), nullable=True)
    sort_order = Column(Integer, default=0)   # 新增(2026-08-20 Q5)：拖拽排序
    created_at = Column(DateTime, default=datetime.now)

    projects = relationship("Project", back_populates="theme", cascade="all, delete-orphan")
```

### 5.3 校验规则（强约束）

| 字段 | 校验规则 |
| :--- | :--- |
| `color_hex` | 必须为 `#RRGGBB` 格式，且属于预定义的 16 种标准色列表（见附录 A）。 |
| `marker_shape` | 必须为 `circle`、`diamond`、`square`、`triangle` 之一。 |
| `raw_end_row` | 若为 NULL，表示仅取 `raw_start_row` 这一行数据。 |
| `raw_end_col_letter` | 若为 NULL，表示仅取 `raw_start_col_letter` 这一列数据。 |
| `status` | 仅允许 `ACTIVE` 或 `MISSING`。 |

---

## 第 6 章 UI 交互架构（冻结版）

### 6.1 核心界面组件清单

| 组件 | 功能 | 架构约束 |
| :--- | :--- | :--- |
| ThemeTreeWidget (左侧树) | 按主题分组展示图表项目。 | 项目状态颜色标识：🟢 黑色（ACTIVE）/ 🔴 灰色（MISSING）。 |
| ChartListView (中间列表) | 展示项目名称、文件路径简写、状态标签。 | 右键菜单：重新定位、删除、打开所在文件夹。 |
| ImportView (导入页) | 选择原始 Excel，设定行列范围。 | 行列选择器：QSpinBox (行) + QLineEdit (列字母)，带有效性校验（列字母必须为 A-ZZZ）。 |
| ConfigView (配置页) | 添加/删除系列，选择 X/Y 列，设置颜色/形状。 | 颜色选择器：仅 16 个色块按钮（取色器禁用）。形状选择器：仅 4 个图标按钮。 |
| ChartPreviewWidget (预览) | ECharts 实时渲染散点图。 | 降采样契约：若数据 > 5000 行，自动仅传输前 5000 点给 ECharts（通过 `data.slice(0, 5000)`）。提示标签：显示 "预览仅展示前 5000 个数据点，导出时将包含全部数据"。 |
| ExportDialog (导出对话框) | 确认导出路径，显示进度条 + 取消按钮。 | 进度回调：实时更新 0-100% 进度。取消逻辑：点击取消 → 设置 `_kill_flag = True` → 工作线程抛出 `OperationCancelledException` → 执行回滚。 |

### 6.2 列选择器最终交互方案（对应 1 万列场景）

由于用户决策为"输入起始列和结束列"（决策 3），无需实现复杂的搜索下拉框。UI 设计如下：

```
┌──────────────────────────────────────┐
│  列范围设置                          │
│  起始列: [ A ]  结束列: [   ] (可选) │
│  (输入字母，如 A, AA, ZZZ)          │
│  💡 提示：留空结束列表示仅取一列数据 │
└──────────────────────────────────────┘
```

**校验逻辑**：

- 输入必须为纯字母（A-Z，不区分大小写，自动转大写）。
- 起始列 ≤ 结束列（若结束列不为空）。
- 若结束列为空，仅读取起始列这一列数据。
- 超出 Excel 最大列数（XFD）时，弹窗提示。

### 6.3 灰色不可用项目交互（对应决策 4 补充）

当 `Project.status = 'MISSING'` 时：

```
┌─────────────────────────────────────────┐
│  ⚠️ 我的图表项目 (2 个，其中 1 个缺失) │
│  ┌─────────────────────────────────┐   │
│  │ 🔴 质量报告_散点图  ❌ 文件缺失 │   │
│  │    路径: D:\data\report.xlsx   │   │
│  │    [重新定位]  [🗑️ 删除]       │   │
│  ├─────────────────────────────────┤   │
│  │ 🟢 销售分析_散点图  ✅ 正常    │   │
│  │    [双击编辑]                   │   │
│  └─────────────────────────────────┘   │
```

**强制行为**：

- 灰色项目 **不可双击**（无法进入编辑）。
- 仅 "重新定位" 和 "删除" 按钮可用。
- 点击"删除" → 弹出二次确认框："确定要删除该图表项目吗？（磁盘文件不会被删除）"。
- 点击"重新定位" → 弹出文件选择器，用户选中新文件后自动校验是否包含 `raw data` 页。

### 6.4 新增交互规范（2026-08-20 客户决策 Q1~Q6）

> 本小节为客户 2026-08-20 确认的 6 项 UI 开放性问题决策，完整版见第 14 章。

| 功能 | 规范描述 |
| :--- | :--- |
| 深色模式 | **暂不支持**（Q1-A），仅实现浅色主题。 |
| 项目列表多选 | **支持多选**（Q2-B）：Ctrl+点击逐选、Shift+点击范围选；选中行高亮（背景色 `#E8F4FD`）；顶部显示"已选 N 个项目"及批量操作按钮；MISSING 项目不可选中。 |
| 预览缩放/平移 | **支持缩放平移**（Q3-B）：ECharts 启用 `dataZoom` 滑块 + `toolbox` 缩放/复位按钮，支持框选放大。 |
| 导出图片 | **支持 .png/.svg 导出**（Q4-B）：新增"导出为图片"按钮，弹出 ExportImageDialog（格式/尺寸/路径），后端实现 ECharts 截图。 |
| 主题拖拽排序 | **支持拖拽**（Q5-B）：主题树节点拖拽排序，松开更新数据库 `sort_order`；仅排序，不支持跨主题移动。 |
| 最近项目 | **显示最近 5 个项目**（Q6-B）：按 `last_opened_at` 降序，点击直接打开，缺失项目显示 ❌ 且不可点击。 |

**新增 UI 组件清单**：

| 组件名称 | 类型 | 说明 |
| :--- | :--- | :--- |
| BatchActionToolbar | QToolBar | 多选后出现的批量操作工具栏（批量导出 / 批量删除） |
| ChartZoomControl | QHBoxLayout | 预览区缩放控件（放大 / 缩小 / 复位） |
| ExportImageDialog | QDialog | 导出图片设置对话框（格式 / 尺寸 / 路径） |
| RecentProjectsWidget | QWidget | 启动页最近项目列表 |
| DragDropTreeWidget | QTreeWidget | 支持拖拽排序的主题树 |

---

## 第 7 章 导出覆盖策略与取消机制（方案 A 细化）

### 7.1 写前备份流程（必须实现）

```python
def export_project(project_id: int, target_path: str):
    # 1. 若目标文件已存在，创建临时备份
    backup_path = None
    if os.path.exists(target_path):
        backup_path = target_path + ".backup_" + datetime.now().strftime("%Y%m%d%H%M%S")
        shutil.copy2(target_path, backup_path)

    try:
        # 2. 写入新文件 (含 raw data + 散点图)
        workbook = create_workbook(project_id)
        workbook.save(target_path + ".tmp")  # 先写临时文件

        # 3. 校验临时文件是否完整 (openpyxl 可正常打开)
        load_workbook(target_path + ".tmp").close()

        # 4. 替换正式文件
        if os.path.exists(target_path):
            os.remove(target_path)
        os.rename(target_path + ".tmp", target_path)

        # 5. 删除备份 (若存在)
        if backup_path and os.path.exists(backup_path):
            os.remove(backup_path)

    except Exception as e:
        # 6. 异常回滚：恢复原文件 (若备份存在)
        if backup_path and os.path.exists(backup_path):
            shutil.copy2(backup_path, target_path)
            os.remove(backup_path)
        # 删除临时文件 (若存在)
        if os.path.exists(target_path + ".tmp"):
            os.remove(target_path + ".tmp")
        raise OperationFailedException(f"导出失败: {str(e)}")
```

### 7.2 取消机制集成

```python
class ExportWorker(QThread):
    progress_signal = Signal(int)

    def __init__(self, project_id, target_path):
        super().__init__()
        self._kill_flag = False
        self.project_id = project_id
        self.target_path = target_path

    def cancel(self):
        self._kill_flag = True

    def run(self):
        try:
            # 在循环体内每 1000 行检查一次
            for idx, row in enumerate(data_rows):
                if self._kill_flag:
                    raise OperationCancelledException("用户取消导出")
                # 写入行数据...
                self.progress_signal.emit(int(idx / total * 100))
        except OperationCancelledException:
            # 回滚：删除临时文件，恢复备份
            self._rollback()
            self.progress_signal.emit(-1)  # -1 表示取消完成
```

### 7.3 IStoppable 接口（强制接口定义）

所有耗时类（`ExcelHandler`、`ChartBuilder`、`ExcelExportService`）必须实现以下接口：

```python
# src/core/stoppable.py
class IStoppable:
    """耗时任务统一取消接口（强制实现）"""

    def __init__(self):
        self._kill_flag = False

    def cancel(self):
        """请求取消：设置终止标志"""
        self._kill_flag = True

    def should_cancel(self) -> bool:
        """检查是否收到取消请求（每 1000 行 / 每写入一个 Sheet 调用一次）"""
        return self._kill_flag
```

### 7.4 回滚契约（三要素）

捕获 `OperationCancelledException` 或导出异常后，**必须依次执行**：

1. **删除未写完的临时文件**（`.tmp`）。
2. **回滚数据库事务**（未提交的 `Project` / `Series` 记录回滚）。
3. **还原原文件**（覆盖场景下，若存在备份则恢复）。

---

## 第 8 章 性能阈值与验收标准（修正版）

| 指标 | 原计划 | 修正后（基于用户决策） |
| :--- | :--- | :--- |
| Excel 解析（10 万行） | ≤ 30 秒 | 不变（但因只解析指定列区间，实际性能会显著优于 30 秒）。 |
| 预览渲染（10 万点） | ≤ 500ms (1000点) | 降采样契约：> 5000 行时预览仅展示前 5000 点，渲染 ≤ 1 秒。<br>提示：预览区显示 "预览仅展示前 5000 个数据点"。 |
| 导出 Excel（10 万行） | 未定义 | 新增目标：生成 10 万行 × 2 列的原生散点图文件 ≤ 20 秒。 |
| 数据库体积 | ≤ 1GB | 修正为 ≤ 100MB（因不存原始数据）。 |
| 应用启动 | ≤ 3 秒 | 新增验证：启动时自动后台探活所有 `Project.source_file_path`，异步标记状态。 |

---

## 第 9 章 开发计划调整（基于冻结架构）

### 9.1 Week 1 调整（数据模型）

| 原任务 | 调整后任务 | 变更原因 |
| :--- | :--- | :--- |
| 设计 Datasource 模型 | 废弃，改为设计 Project 模型（不含任何数据存储字段）。 | 用户决策不存原始数据。 |
| 设计 Chart 模型 | 合并至 Project 模型（图表元数据归入 Project 表）。 | 简化表结构。 |
| 设计 Theme + Series 模型 | 不变。 | — |
| 编写数据库初始化 | 增加 status 字段和索引。 | 支持文件缺失状态标记。 |

### 9.2 Week 2 调整（Excel 解析）

| 原任务 | 调整后任务 | 变更原因 |
| :--- | :--- | :--- |
| ExcelHandler 流式读取全量数据 | 改为按列区间读取：`iter_rows(min_row, max_row, min_col, max_col)`。 | 用户决策指定列范围。 |
| 测试 10 万行×1 万列解析 | 增加测试：仅读取 2 列时性能应 ≤ 5 秒。 | 验证列区间解析优化。 |

### 9.3 Week 3 调整（核心能力）

| 原任务 | 调整后任务 | 变更原因 |
| :--- | :--- | :--- |
| ChartBuilder 构建散点图 | 不变，但数据源改为从 `raw data` 页读取（而非原始 Excel）。 | 生成新文件流程。 |
| ExcelExportService | 重写：从"修改原文件"改为"生成全新文件"，含 raw data + Chart View 两页。 | 用户决策导出结构。 |

### 9.4 Week 6 调整（导出功能）

| 原任务 | 调整后任务 | 变更原因 |
| :--- | :--- | :--- |
| 嵌入到原 Excel | 完全移除，替换为"生成新 Excel 文件"。 | 用户决策。 |
| 导出到新 Excel | 保留，且作为唯一导出模式。 | — |
| 批量导出 | 不变，但改为每个图表生成独立文件（同一主题下打包为文件夹）。 | 多个独立文件便于管理。 |
| 写前备份机制 | 新增开发任务（+0.5 天）。 | 覆盖方案 A 的安全保障。 |
| 灰色项目删除按钮 | 新增开发任务（+0.3 天）。 | 用户补充决策。 |

> 注：完整 Week 1~8 排期以《03-开发计划书.md》为准，本表仅列出受冻结架构影响而调整的任务。

### 9.5 增量任务（2026-08-20 UI 决策新增，合计 +4.0 天）

| 周次 | 原计划 | 因 UI 决策新增任务 | 调整后工期 |
| :--- | :--- | :--- | :--- |
| Week 1 | 数据库 Schema + ORM | + `sort_order` 字段 + `last_opened_at` 字段 + 最近项目查询逻辑 | +0.5 天 |
| Week 3 | ChartBuilder + ExcelExportService | + ECharts `dataZoom` + `toolbox` 配置 | +0.5 天 |
| Week 4 | UI 主窗口 | + 主题树拖拽排序 | +0.5 天 |
| Week 5 | 配置面板 + 预览 | + 项目列表多选 + 批量操作工具栏 | +1.0 天 |
| Week 6 | 导出唯一模式 + 覆盖策略 | + "导出为图片"功能（PNG/SVG） | +1.0 天 |
| Week 7 | 集成测试 + 验收 | + 新增功能测试用例 | +0.5 天 |
| **总计** | — | — | **+4.0 天** |

> 说明：第 14 章 2.3 节的"功能增量 +3.5 天"不含 Week 7 测试用例；本表 +4.0 天为含测试用例的**最终总增量**，以本表为准。

---

## 第 10 章 变更管理（ACR 流程）

本文件为**冻结文档**，任何变更必须走以下流程：

1. 需求方或开发方提出变更诉求，填写《架构变更请求（ACR）》表单（描述变更内容、影响范围、涉及模块、工作量估算）。
2. 架构师评估变更对冻结决策的影响，给出技术可行性结论。
3. 项目经理组织评审会，**客户签字确认**后方可实施。
4. 变更确认后，由文档维护者更新本文件，并升版本号、记录变更历史。
5. 未走 ACR 流程的变更一律视为违规，不得进入代码。

---

## 第 11 章 附录 A：16 种 Excel 标准色（UI 调色板硬编码）

以下 16 种颜色为 UI 颜色选择器的**全部选项**，禁止引入任何其他颜色（包括渐变色）：

| 序号 | 颜色名称 | Hex 值 | Excel 索引色 |
| :--- | :--- | :--- | :--- |
| 1 | 红色 | #FF0000 | 3 |
| 2 | 深红 | #C00000 | 9 |
| 3 | 橙色 | #FFC000 | 44 |
| 4 | 金色 | #FFD966 | 45 |
| 5 | 黄色 | #FFFF00 | 6 |
| 6 | 亮绿 | #92D050 | 48 |
| 7 | 绿色 | #00B050 | 10 |
| 8 | 深绿 | #006100 | 17 |
| 9 | 青色 | #00B0F0 | 40 |
| 10 | 深蓝 | #0070C0 | 41 |
| 11 | 蓝色 | #0000FF | 5 |
| 12 | 紫色 | #7030A0 | 54 |
| 13 | 洋红 | #FF00FF | 7 |
| 14 | 粉色 | #FF99CC | 38 |
| 15 | 棕色 | #A52A2A | 53 |
| 16 | 黑色 | #000000 | 1 |

---

## 第 12 章 附录 B：代码强制校验规则（Pydantic 风格）

```python
# src/models/validators.py
from enum import Enum

class MarkerShape(str, Enum):
    CIRCLE = "circle"
    DIAMOND = "diamond"
    SQUARE = "square"
    TRIANGLE = "triangle"

EXCEL_SAFE_COLORS = [
    "#FF0000", "#C00000", "#FFC000", "#FFD966",
    "#FFFF00", "#92D050", "#00B050", "#006100",
    "#00B0F0", "#0070C0", "#0000FF", "#7030A0",
    "#FF00FF", "#FF99CC", "#A52A2A", "#000000"
]

def validate_color_hex(color: str) -> bool:
    """校验颜色是否为 16 种标准色之一"""
    return color.upper() in [c.upper() for c in EXCEL_SAFE_COLORS]

def validate_marker_shape(shape: str) -> bool:
    """校验点形状是否为 4 种之一"""
    return shape.lower() in [item.value for item in MarkerShape]

def validate_col_letter(col: str) -> bool:
    """校验列字母是否合法 (A-ZZZ)"""
    import re
    return bool(re.match(r'^[A-Z]{1,3}$', col.upper()))
```

---

## 第 13 章 签署确认

本架构冻结文件已包含全部客户决策，并获得以下确认：

| 角色 | 确认内容 | 日期 |
| :--- | :--- | :--- |
| 客户 | 6 项核心决策（问题 1-6）及覆盖方案（A）、删除按钮均已确认。 | 2026-08-17 |
| 架构师 | 所有业务决策均已转化为可执行的技术规范，无歧义。 | 2026-08-17 |
| 项目经理 | 开发计划已根据本架构调整（见第 9 章），资源分配合理。 | 2026-08-17 |
| 客户 | 确认 6 项 UI 决策（Q1~Q6，见第 14 章）。 | 2026-08-20 |
| 客户 | 确认新增功能范围及工作量评估（+4.0 天）。 | 2026-08-20 |
| 架构师 | 确认数据库变更（themes.sort_order / projects.last_opened_at）兼容冻结架构。 | 2026-08-20 |
| 项目经理 | 确认开发计划调整（第 9.5 节，+4.0 天）。 | 2026-08-20 |
| UI 设计师 | 确认 UI 设计稿最终版（第 14 章）。 | 2026-08-20 |

---

## 第 14 章 UI 设计规范（冻结版，2026-08-20 确认）

> **确认日期**：2026-08-20 ｜ **确认状态**：✅ 6 项 UI 开放性问题已完成客户决策。
> 本章为 2026-08-20 客户 UI 设计稿确认的**完整固化稿**，与前述章节冲突时**以本章为准**。

### 14.1 UI 开放性问题 — 客户决策汇总

| 问题 | 客户选择 | 设计影响 |
| :--- | :--- | :--- |
| Q1: 深色模式 | A. 暂不支持 | 仅实现浅色主题，按现有设计稿推进 |
| Q2: 项目列表多选 | B. 支持多选（Ctrl/Shift） | 需增加多选交互、批量删除/导出功能 |
| Q3: 预览区缩放/平移 | B. 支持缩放平移 | ECharts 需启用 dataZoom 和 toolbox 控件 |
| Q4: 图表导出图片 | B. 支持 .png/.svg 导出 | 增加"导出为图片"按钮，后端需实现 ECharts 截图 |
| Q5: 主题拖拽排序 | B. 支持拖拽 | 主题树需实现拖拽排序，数据库增加 sort_order 字段 |
| Q6: 最近项目 | B. 显示最近 5 个项目 | 启动页或菜单中增加"最近打开"列表 |

### 14.2 因决策产生的增量需求

#### 14.2.1 数据库变更（对应 Q5 / Q6）

```sql
-- Q5：themes 表增加 sort_order 字段（已并入第 5 章）
ALTER TABLE themes ADD COLUMN sort_order INTEGER DEFAULT 0;

-- Q6：projects 表增加 last_opened_at 字段（已并入第 5 章）
ALTER TABLE projects ADD COLUMN last_opened_at TIMESTAMP;
```

#### 14.2.2 UI 新增功能清单

| 功能 | 对应决策 | 新增组件/交互 |
| :--- | :--- | :--- |
| 项目列表多选 | Q2 | Ctrl/Shift 多选 + 批量操作工具栏 |
| 预览缩放平移 | Q3 | ECharts dataZoom 滑块 + toolbox 缩放按钮 |
| 导出为图片 | Q4 | "导出图片" 按钮（PNG/SVG 二选一） |
| 主题拖拽排序 | Q5 | 主题树节点拖拽（仅排序，不支持跨主题移动/复制） |
| 最近项目列表 | Q6 | 启动时显示最近 5 个项目（或菜单中展示） |

#### 14.2.3 工作量评估更新

| 增量功能 | 预估工时 | 影响阶段 |
| :--- | :--- | :--- |
| 多选 + 批量操作 | +1.0 天 | Week 5 |
| ECharts dataZoom + toolbox | +0.5 天 | Week 3 |
| ECharts 截图导出（PNG/SVG） | +1.0 天 | Week 6 |
| 主题树拖拽排序 | +0.5 天 | Week 4 |
| 最近项目功能 | +0.5 天 | Week 1 |
| 新增功能测试用例 | +0.5 天 | Week 7 |
| **合计** | **+4.0 天** | — |

### 14.3 更新后的 UI 预览（含新增功能）

#### 14.3.1 项目列表（多选状态）

```
┌──────────────────────────────────────────────────────────────────────┐
│  📋 销售主题 (4 个项目)           [批量导出] [批量删除] [刷新]   │
├──────────────────────────────────────────────────────────────────────┤
│  □ 状态  项目名称              文件路径              操作          │
│  ├──────────────────────────────────────────────────────────────────┤ │
│  │ ☑ ✅   销售数据分析_Scatter  D:\data\销售.xlsx   [✏️][🗑️]    │ │ ← 选中状态（高亮）
│  │ ☑ ✅   月度趋势_Scatter      D:\data\月度.xlsx   [✏️][🗑️]    │ │ ← 选中状态
│  │ ☐ ✅   年度总结_Scatter      D:\data\年度.xlsx   [✏️][🗑️]    │ │ ← 未选中
│  │ ☐ ❌   测试数据_Scatter      D:\data\测试.xlsx   [🔗][🗑️]    │ │ ← 缺失（不可选）
│  └──────────────────────────────────────────────────────────────────┘ │
│  已选: 2 个项目                                     [导出选中]     │
└──────────────────────────────────────────────────────────────────────┘
```

#### 14.3.2 预览区（含缩放控件）

```
┌──────────────────────────────────────────────────────────────────────┐
│  📈 散点图预览                     [🔍+] [🔍-] [↕复位] [📷导出图片] │
├──────────────────────────────────────────────────────────────────────┤
│  ┌────────────────────────────────────────────────────────────────┐ │
│  │  [  ████████████████████████████████████████████████  ]      │ │ ← dataZoom 滑块
│  │    ●    ●    ●    ●    ●    ●    ●    ●   ← ECharts        │ │
│  │      ●       ●    ●    ●    ●    ●                         │ │
│  │    ●    ●    ●    ●    ●    ●    ●    ●   ← 渲染区         │ │
│  │    ════════════════════════════════════════════              │ │
│  │     X 轴标签（用户自定义）                                  │ │
│  │  [🔍] [🔍+] [⟳] [📷]   ← ECharts 内置工具箱               │ │
│  └────────────────────────────────────────────────────────────────┘ │
│  📍 预览仅展示前 5,000 个数据点  |  导出将包含全部 12,847 点     │
│  ⏱ 渲染耗时: 0.8s  |  系列数: 2  |  缩放: 75%                   │
└──────────────────────────────────────────────────────────────────────┘
```

#### 14.3.3 导出图片对话框

```
┌─────────────────────────────────────────────────────────────────┐
│  📷 导出为图片                                      [×] 关闭 │
├─────────────────────────────────────────────────────────────────┤
│  导出当前图表预览                                              │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │  [  预览缩略图  ]                                       │ │
│  │  当前显示: 2 个系列 · 3,245 个数据点                    │ │
│  └───────────────────────────────────────────────────────────┘ │
│  导出设置                                                      │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │  格式: [▼ PNG ▼]  (PNG / SVG)                          │ │
│  │  分辨率: [1920 × 1080]  [自定义尺寸...]                │ │
│  │  保存位置: [D:\data\charts\               ] [浏览...]   │ │
│  │  文件名称: [销售数据_散点图_20260820]                   │ │
│  └───────────────────────────────────────────────────────────┘ │
│  [取消]  [导出]                                               │
└─────────────────────────────────────────────────────────────────┘
```

#### 14.3.4 应用启动页（最近项目）

```
┌──────────────────────────────────────────────────────────────────────┐
│  📊 Excel 散点图生成器                                              │
├──────────────────────────────────────────────────────────────────────┤
│                    ┌──────────────────────────────┐                  │
│                    │   📊 最近打开的项目          │                  │
│                    │   1. 销售数据分析_Scatter   │                  │
│                    │      D:\data\销售.xlsx      │                  │
│                    │      2026-08-20 14:30       │                  │
│                    │   2. 质量报告_Scatter       │                  │
│                    │      D:\data\质量.xlsx      │                  │
│                    │      2026-08-19 10:15       │                  │
│                    │   3. 月度趋势_Scatter       │                  │
│                    │      D:\data\月度.xlsx      │                  │
│                    │      2026-08-18 16:45       │                  │
│                    │   4. 测试数据_Scatter  ❌   │                  │
│                    │      (文件缺失)             │                  │
│                    │   5. 年度总结_Scatter       │                  │
│                    │      D:\data\年度.xlsx      │                  │
│                    │      2026-08-17 09:20       │                  │
│                    │    [清空最近记录]            │                  │
│                    └──────────────────────────────┘                  │
│                    [📂 导入新数据]   [📁 浏览全部项目]              │
└──────────────────────────────────────────────────────────────────────┘
```

### 14.4 新增 UI 交互规范（强制）

| 功能 | 规范描述 |
| :--- | :--- |
| 多选操作 | 项目列表支持 Ctrl+点击（逐选）和 Shift+点击（范围选）；选中项目行高亮（背景色 `#E8F4FD`）；顶部显示"已选 N 个项目"及批量操作按钮；MISSING 项目不可被选中。 |
| 预览缩放 | ECharts 启用 dataZoom 滑块（图表底部）+ toolbox 缩放/复位按钮；用户可框选放大区域。 |
| 导出图片 | "导出为图片"按钮支持 PNG（位图）/ SVG（矢量图）；导出前展示预览缩略图和尺寸设置（预设 1920×1080 / 3840×2160 / 自定义）。 |
| 主题拖拽 | 主题树支持拖拽排序，拖拽时显示虚线插入指示线，松开后更新数据库 `sort_order`；**仅排序，不支持跨主题移动**。 |
| 最近项目 | 启动时首页/菜单显示最近打开的 5 个项目（按 `last_opened_at` 降序）；点击直接打开；缺失项目显示 ❌ 且不可点击。 |

### 14.5 新增 UI 组件清单

| 组件名称 | 类型 | 说明 |
| :--- | :--- | :--- |
| BatchActionToolbar | QToolBar | 多选后出现的批量操作工具栏（批量导出/批量删除） |
| ChartZoomControl | QHBoxLayout | 预览区缩放控件（放大/缩小/复位） |
| ExportImageDialog | QDialog | 导出图片设置对话框（格式/尺寸/路径） |
| RecentProjectsWidget | QWidget | 启动页最近项目列表 |
| DragDropTreeWidget | QTreeWidget | 支持拖拽排序的主题树 |

---

## 附录 C：文档变更记录

| 版本 | 日期 | 变更说明 | 变更来源 |
| :--- | :--- | :--- | :--- |
| v3.0 | 2026-08-17 | 架构冻结初版（6 项核心决策）固化稿。 | 客户需求澄清会议 |
| v3.1 | 2026-08-20 | 新增 6 项 UI 决策（Q1~Q6）：多选/缩放/导出图片/主题拖拽/最近项目；新增 `themes.sort_order`、`projects.last_opened_at` 字段；开发计划增量 +4.0 天；新增第 14 章 UI 设计规范（冻结版）。 | 客户 UI 设计稿确认 |

