# 需求差距分析与改进建议（基于真实附件）

> **分析日期**：2026-08-20
> **依据**：`sample_data/销售数据2026.xlsx`（样例）、`LP G2 110W Perofrmance_NF-2026-07-01--40C 085905=Report-update.xlsx`（原始报表）、`performace ESR3 20260702 -no.1.xlsx`（手工散点图参照）、`销售数据2026_Scatter_20260820_221654.xlsx`（现软件输出）
> **结论先行**：现有软件（v3.1 规范）是「简单表格 → 单图表、手动系列」模型；真实需求是「测试报表 → 自动分组多系列、多图表、多 Sheet」模型。**结构差异巨大，需做架构级增强，而非小修小补。**

---

## 一、附件数据画像（实测）

### 1. 原始报表 `LP G2 ... Report-update.xlsx`
- 单 Sheet「Sheet1」，4195 行 × 约 39 个数据列。
- **第 1~16 行是元数据前导**：报告创建人（Zhengjie Shan / signify.com）、测试设备（YOKOGAWA WT1802 功率计、HP 34970A 万用表、ITECH IT8511A 电子负载）、温箱、电源等。
- **第 16 行**：分组标签（Test profile / Chamber / Mains / Load-Output）。
- **第 17 行**：真正的表头（test no.、Mains Voltage、Mains Freq.、Set Temp.、Interface 0-10V/DALI/Rset/SimpleSet、LOAD 1、Light out、DC LOAD、Temperature、Voltage RMS、Current RMS、Power、PF、THD、Ripple…）。
- **第 18 行**：单位行（[ V ]、[ Hz ]、[ C deg. ]、[mA]、W、%、DAY、TIME…）。
- **第 19 行起**：数据行。
- 无内嵌图表（纯原始数据）。

### 2. 手工散点图 `performace ESR3 ... no.1.xlsx`
- **2 个 Sheet**：`25-55C`、`-40C`（温度条件）。
- **每个 Sheet 内嵌 18 张 ScatterChart**。
- **每张图 10~22 个系列**，系列名按测试条件自动拼出，例如：
  - `1050mA-230V-50HZ-25C`（电流 1050mA / 电压 230V / 频率 50Hz / 温度 25℃）
  - `Pout VS THD-1050mA-230V-50HZ--40C`（输出功率 vs 总谐波失真）
  - 部分图还含 `max current` / `min current` 汇总系列。
- X 引用为单列分块，例如 `Sheet1!$Z$25:$Z$50`（列 Z，第 25~50 行是一个条件的数据块，下一块 $Z$51:$Z$77 是下一个条件）。

### 3. 现软件输出 `销售数据2026_Scatter_...xlsx`
- 正确产出 `raw data` + `Chart View` 两页，含原生 ScatterChart（1 个系列「系列 1」，X=列 A、Y=列 B）。
- **但只适用于「表头在第 1 行、单一矩形表格」的简单数据**，无法解析上述真实报表。

---

## 二、核心差距（现状 vs 需求）

| 维度 | 现状（v3.1 规范） | 真实需求（来自附件） |
| :--- | :--- | :--- |
| 表头位置 | 硬编码第 1 行 | 表头在第 17 行，前 16 行是元数据 |
| 表头结构 | 单行表头 | 表头 + 单位行（两层） |
| 数据布局 | 单一矩形表格 | 按测试条件纵向分块的报表 |
| Sheet | 只取第一个 sheet | 多 Sheet（25-55C / -40C）需分别处理 |
| 系列来源 | 用户手动添加 | 按「电流×电压×频率×温度」组合自动生成 |
| 系列命名 | 手动输入 | 模板自动生成（`{电流}mA-{电压}V-{频率}HZ-{温度}C`） |
| 图表数量 | 一项目 = 一张图 | 一项目 = 多张图（实测 18 张/sheet） |
| 系列数量 | 少量 | 单图 10~22 个系列 |
| 图表类型 | 单一 X/Y 散点 | 多组指标对（Pout vs THD、Current vs Voltage…） |

---

## 三、改进建议（按优先级）

### P0 —— 数据解析层（不解决则无法使用）

1. **表头行自动识别 + 手动可指定**
   自动扫描前 N 行，把「文本单元格最多」的行识别为表头；同时允许用户手动输入表头行号（本例为 17）。

2. **支持元数据前导 + 两层表头（名称行 + 单位行）**
   识别并跳过报告前导的元数据区；把单位行合并进列名，如 `Voltage RMS [V]`、`Current RMS [A]`。

3. **多 Sheet 支持**
   一个文件多个温度条件 sheet，可单选或批量处理全部 sheet。

4. **导入后数据预览确认**
   解析后先展示「表头 + 前 N 行」预览，让用户确认表头行、单位行、数据起始行是否正确，再进入配置。

### P1 —— 系列自动生成（核心价值所在）

5. **按测试条件自动分组生成系列**
   让用户指定「分组条件列」（如 Mains Voltage、Mains Freq.、Set Temp.、LOAD 1），软件按这些列的唯一组合自动拆分出 N 个系列。

6. **系列命名模板**
   支持 `{电流}mA-{电压}V-{频率}HZ-{温度}C` 这类模板，自动生成系列名（对齐手工图命名）。

7. **每个系列 = 一个数据块（分块识别）**
   识别「同一条件占据连续若干行」的纵向分块，自动计算每个系列的数据区间（等价于手工图里 `$Z$25:$Z$50`、`$Z$51:$Z$77` 的分块引用）。

### P2 —— 多图表能力

8. **一项目支持多张图**
   把「一项目 = 一张图」升级为「一项目 = N 张图」，每张图 = 一对 (X 指标, Y 指标) + 系列集合。

9. **图表模板 / 一键多图**
   预定义常用指标对（如 `Pout vs THD`、`Current vs Voltage`、`PF vs Load`），让用户勾选后一次生成多张图。

10. **汇总系列**
    支持在每张图末尾追加 `max / min` 等汇总系列（对齐手工图中的 max current / min current）。

### P3 —— 导出与性能

11. **批量多 Sheet / 多图导出**
    一个源文件 → 生成一个含多 Sheet、每 Sheet 多图的原生 `.xlsx`。

12. **系列数量上限放宽**
    单图需支持 20+ 系列，验证 openpyxl 在 20~22 系列下的生成性能与图例布局。

### P4 —— 体验

13. **列别名映射**
    把原始列（Z/V/T/N… 或 Voltage RMS/THD 等）映射为友好名，避免用户记列号。

14. **图例管理**
    20+ 系列时图例位置（右侧/底部/关闭）可配置。

---

## 四、对数据库与架构的影响

| 项 | 说明 |
| :--- | :--- |
| 新增 `charts` 表 | 一个 project 下多张图；`series` 增加 `chart_id` 外键。 |
| `projects` 增加字段 | 表头行号 `header_row`、单位行号 `unit_row`、数据起始行、分组条件列、系列命名模板。 |
| 解析层重构 | `ExcelHandler` 从「固定第 1 行表头」改为「可配置表头行 + 单位行 + 分块分组」。 |
| UI 新增页面 | 「表头/分组设置」页（选表头行、单位行、分组列、命名模板）+「图表模板」页（多指标对勾选）。 |

> 说明：以上属于架构级变更，按冻结规范第 10 章应走《架构变更请求（ACR）》流程。

---

## 五、需客户确认的关键问题 → 已答复（2026-08-20）

| # | 问题 | 客户答复 |
| :--- | :--- | :--- |
| 1 | X/Y 含义 | 同时列出行和列，用户自己定义哪个是 X、哪个是 Y |
| 2 | 是否复刻 18 张图 | 不复刻；生成可增删改查的多图表内容，用户决定做几张图 |
| 3 | 系列分组键 | 由用户自己定义，不是固定四参数组合 |
| 4 | LP G2 与 ESR3 关系 | LP G2=原始数据，ESR3=散点图文件；把原始数据 copy 到 Sheet1，散点图放 Sheet2，之后不再依赖原始数据 |
| 5 | 是否汇总 | 不汇总，直接画原始散点；起止行列由用户输入 |

> 以上答复已纳入 `项目开发规范.md` 第 15 章（v3.2 修订），本文件的 P0~P4 建议据此收敛为：
> **P0 精确行列范围**（真实 Excel 行列号，无表头假设）+ **P1 多图表 CRUD**（新增 charts 表）+ **P2 系列自定义（含每系列行范围）** + **自包含输出（Sheet1=raw data + 每图一 Sheet）**；不再需要「自动分组」「统计汇总」。

---

## 六、已实现功能记录

> 以下记录已落地的功能改进，便于追溯需求→实现的对应关系。

### 2026-08-24 · v3.2.1

#### ✅ 图表复制功能

| 项 | 说明 |
| :--- | :--- |
| **需求来源** | 用户反馈：创建含多系列的图表后，若下一张图结构类似（如不同温度条件），需从零重建所有系列，操作重复且易出错。 |
| **实现方式** | 在 `ChartListWidget` 新增右键菜单"📋 复制图表" + 底部按钮栏"📋 复制图表"按钮。 |
| **复制范围** | 整张图表深复制：Chart 元数据（名称、标题、轴标签、刻度、网格/连线设置）+ 全部 Series 记录（系列名、X/Y 列、行范围、颜色、形状、大小）+ 全部 ChartLimit 限值规则。 |
| **副本命名** | 新图表名称 = 原名称 + " 副本"后缀，用户可在"图表选项"标签页自由修改。 |
| **涉及文件** | `src/ui/widgets/project_list.py`（右键菜单 + `copyRequested` 信号）、`src/ui/main_window.py`（`_copy_chart` 方法 + 按钮）。 |

#### ✅ openpyxl `inf`/`nan` 兼容修复

| 项 | 说明 |
| :--- | :--- |
| **问题现象** | 导入 LED 性能测试 Excel 文件时崩溃：`ValueError: invalid literal for int() with base 10: 'inf'`。 |
| **根因** | openpyxl 内部 `_cast_number` 函数将 `"inf"` 字符串传给 `int()`，而 `int("inf")` 会抛出 ValueError。这是 openpyxl 的已知缺陷。 |
| **修复方式** | 在 `excel_handler.py` 中 monkey-patch `_cast_number`，在调用 `int()` 前先检测 `"inf"` / `"-inf"` / `"nan"` 并转为 `float()`。 |
| **涉及文件** | `src/services/excel_handler.py`（第 16~26 行，补丁代码）。 |

#### ✅ 当前图表高亮显示

| 项 | 说明 |
| :--- | :--- |
| **需求来源** | 用户反馈：图表列表和项目树中，当前激活的图表没有明显高亮，用户无法直观判断哪个图表正在编辑，容易误操作（如删除错误图表）。 |
| **问题根因** | Qt 默认 `setSelected` 渲染样式（浅色高亮）在白色背景上不醒目，多图表场景下难以区分选中项。 |
| **实现方式** | 为 `ProjectTreeWidget` 和 `ChartListWidget` 添加自定义 QSS 样式表，选中项显示：蓝色左边框（`#1976d2`）+ 浅蓝背景（`#e3f0ff`）+ 加粗字体。悬停未选中项也增加浅蓝背景反馈。 |
| **涉及文件** | `src/ui/widgets/project_list.py`（`ChartListWidget.__init__` 添加 QSS）、`src/ui/widgets/theme_tree.py`（`ProjectTreeWidget.__init__` 添加 QSS）。 |

#### ✅ 项目复制功能

| 项 | 说明 |
| :--- | :--- |
| **需求来源** | 用户反馈：创建含多图表的项目后，若下个项目结构类似（如不同产品型号），需从零重建所有图表和系列，操作重复且易出错。 |
| **实现方式** | 在 `ProjectTreeWidget` 新增右键菜单"📋 复制项目"，右键项目节点触发。 |
| **复制范围** | 整个项目深复制：Project 元数据（名称 + " 副本"后缀、数据范围、导出文件路径）+ 全部 Chart 记录 + 每张 Chart 下的全部 Series + 全部 ChartLimit 限值规则。 |
| **副本命名** | 新项目名称 = 原名称 + " 副本"后缀，可在项目设置中修改。 |
| **设计要点** | 新项目与源项目共享同一 `source_file_path`（导出文件）；副本创建后自动切换为当前项目并刷新树/列表。 |
| **涉及文件** | `src/ui/widgets/theme_tree.py`（`copyProjectRequested` 信号 + `contextMenuEvent`）、`src/ui/main_window.py`（`_copy_project` 方法）。 |

#### ✅ 预览图例右置

| 项 | 说明 |
| :--- | :--- |
| **需求来源** | 用户要求：图表预览中将系列名称显示在图表右端，与导出 Excel 的图例位置保持一致。 |
| **实现方式** | 修改 `PreviewWidget` 中 ECharts 配置：legend 从 `"bottom": 0`（底部横向）改为 `"right": 10, "orient": "vertical"`（右侧纵向），grid 右边距从 40 调整为 160 以预留图例空间。 |
| **涉及文件** | `src/ui/widgets/preview_widget.py`（第 134~136 行）。 |

#### ✅ 交互优化（单击跳转/树展开控制/改名）

| 项 | 说明 |
| :--- | :--- |
| **需求来源** | 客户反馈：① 图表列表需双击才跳转且跳到系列配置页，不便；② 点击项目后全部展开影响选择；③ 树中图表点击后跳转不直观；④ 复制后无法改名。 |
| **修改 1** | 图表列表新增 `chartClicked` 信号，单击即调用 `_on_chart_clicked` → 跳转到**图表预览**标签页（不再跳系列配置）。 |
| **修改 2** | 项目树 `set_data()` 移除全局自动展开循环，改为仅展开当前选中项目所在的父节点组，其余节点由用户手动点击箭头展开。 |
| **修改 3** | 项目树点击图表节点统一走 `_on_chart_selected` → 跳转到图表预览标签页，与图表列表单击行为一致。 |
| **修改 4** | `ProjectTreeWidget` 右键菜单扩展：项目组节点新增"✏️ 改名"（`renameGroupRequested` 信号），项目节点新增"✏️ 改名"（`renameProjectRequested` 信号）+ "📋 复制项目"。`MainWindow` 新增 `_rename_group` / `_rename_project` 方法，弹出 `QInputDialog` 输入新名称并校验重复。 |
| **涉及文件** | `src/ui/widgets/project_list.py`（`chartClicked` 信号）、`src/ui/widgets/theme_tree.py`（改名信号 + 右键菜单 + 移除自动展开）、`src/ui/main_window.py`（`_on_chart_clicked` + `_rename_group` + `_rename_project`）。 |

#### ✅ Bug 修复（树展开/tooltip/高亮持久化）

| 项 | 说明 |
| :--- | :--- |
| **问题 1** | 项目树展开状态在每次 `_refresh_tree()` 刷新后丢失，所有节点折叠。 |
| **根因** | `setExpanded(True)` 在节点调用 `addChild`/`addTopLevelItem` 之前执行，Qt 规定节点未加入树时 `setExpanded` 不生效。 |
| **修复** | `set_data()` 改为两阶段构建：第一阶段创建全部节点并 `addChild`/`addTopLevelItem`，第二阶段统一遍历设置 `setExpanded`。同时保存展开状态（项目 ID 集合）并在重建后恢复。 |
| **问题 2** | 预览 tooltip 悬停散点时显示 JS 函数源码而非 X/Y 数据值。 |
| **根因** | `formatter` 字符串被 `json.dumps(option)` 序列化为 JSON 字符串，ECharts 将其视为纯文本而非可执行函数。 |
| **修复** | HTML 模板增加 `__TOOLTIP_FN__` 占位符，tooltip 函数作为原始 JS 代码通过 `html.replace` 注入，不经过 `json.dumps`。JS 中通过 `opt.tooltip.formatter = tooltipFn` 赋值为函数引用。 |
| **问题 3** | 图表列表单击选中高亮在跳转预览页后消失。 |
| **根因** | `set_charts` 中 `self.clear()` 后逐项 `setSelected(True)`，某些 Qt 渲染时序下选中态未持久应用。 |
| **修复** | 改为先添加全部 item，最后用 `setCurrentRow(idx)` 一次性设置选中行，Qt 确保该行可见且高亮持续到用户切换。 |
| **涉及文件** | `src/ui/widgets/theme_tree.py`（两阶段构建）、`src/ui/widgets/preview_widget.py`（tooltip 模板注入）、`src/ui/widgets/project_list.py`（`setCurrentRow`）。 |

#### ✅ 数据持久化优化（导出后切换数据源/文件迁移支持）

| 项 | 说明 |
| :--- | :--- |
| **需求来源** | 用户反馈：每次更新软件后，数据设置页的文件路径被清空，配置数据丢失。原因是原始文件路径不存入 DB，而导出文件路径虽存入 DB 但导出后未自动切换数据源。 |
| **修改 1** | `_export()` 导出成功后，自动调用 `_run_import()` 从导出文件的 raw data 页重新读取数据，同时更新 ImportView 路径显示，确保数据来源始终指向 DB 中持久化的导出文件。 |
| **修改 2** | `ImportView` 新增 `set_path()` 方法 + 文件路径显示（只读），项目切换和导入完成后自动更新显示。新增"🔄 重新选择文件"按钮（`reimportRequested` 信号），用户可手动选择新文件更新 DB 中的 `source_file_path`。 |
| **修改 3** | `MainWindow._reimport_data()` 处理重新选择文件：打开文件选择器 → 更新 DB 路径 → 用当前项目的行列范围重新读取数据 → 刷新界面。 |
| **涉及文件** | `src/ui/views/import_view.py`（`set_path` + `_reimport` + `reimportRequested` 信号）、`src/ui/main_window.py`（`_reimport_data` + `_export` 数据源切换 + `QFileDialog` 导入）。 |

#### ✅ 数据设置页重构（初始化流程）

| 项 | 说明 |
| :--- | :--- |
| **需求来源** | 用户反馈：更新软件后原始文件路径被清空、配置数据丢失。根因是原始文件路径不存入 DB，而导出文件路径虽存 DB 但导出后未自动切换。 |
| **核心思路** | 引入"初始化"流程：原始文件仅用于初始化时复制到用户指定的保存路径，之后所有操作基于 DB 中持久化的副本，原始文件路径变动不影响数据。 |
| **新增流程** | ① 选择原始文件 → ② 选择保存路径 → ③ 自动复制原始文件到保存路径 → ④ 读取表格行/列数并显示（如"文件总行数：3,984 · 总列数：37（最后行号 3984，最后列 AK）"）→ ⑤ 自动填充结束行/列默认值 → ⑥ 用户配置起止行列 → ⑦ 点击"应用范围并配置图表"存入 DB。 |
| **UI 结构** | 分 4 个区域：①初始化数据文件（原始文件选择 + 初始化按钮）→ ②数据文件信息（保存路径 + 重新初始化按钮 + 表格信息）→ ③配置行列范围 → ④应用按钮。 |
| **涉及文件** | `src/ui/views/import_view.py`（完全重写）、`src/ui/main_window.py`（`_init_data` 方法 + 信号连接）。 |

#### ✅ 数据设置页 Bug 修复（v3.3.1）

| 项 | 说明 |
| :--- | :--- |
| **问题 1** | 行列范围跨项目泄漏：用户在 Project A 配置行列范围后切换到 Project B，ImportView 仍显示 Project A 的范围值，导致用户误以为配置扩展到了所有项目。 |
| **根因** | ① `_import()` 不保存用户配置的行列范围到 DB；② `_on_project_selected()` 不从 DB 恢复 ImportView 控件的行列范围值。 |
| **修复 1** | ImportView 新增 `set_row_col_range()` 方法，用于恢复行列范围到 UI 控件。 |
| **修复 2** | `_import()` 点击"应用范围并配置图表"时，将用户配置的行列范围写入当前项目 DB 记录并 commit。 |
| **修复 3** | `_on_project_selected()` 切换项目时调用 `set_row_col_range()` 从 DB 恢复该项目自己的行列范围。 |
| **涉及文件** | `src/ui/views/import_view.py`（`set_row_col_range`）、`src/ui/main_window.py`（`_import` + `_on_project_selected`）。 |

#### ✅ "Worksheet raw data does not exist" 错误修复

| 项 | 说明 |
| :--- | :--- |
| **问题现象** | 选中项目时弹出"读取失败"对话框，提示 Worksheet raw data does not exist。 |
| **根因** | `_on_project_selected()` 无条件用 `read_raw=True` 读取 "raw data" Sheet，但新初始化的文件只有原始 Sheet（如 Sheet1），"raw data" Sheet 仅导出后才存在。 |
| **修复** | `has_raw_sheet = False` 初始化；打开 workbook 时检测 `RAW_DATA_SHEET in wb.sheetnames`；`_run_import` 使用 `read_raw=has_raw_sheet`，智能选择 Sheet。 |
| **涉及文件** | `src/ui/main_window.py`（`_on_project_selected`）。 |

#### ✅ 项目树选中高亮修复

| 项 | 说明 |
| :--- | :--- |
| **问题现象** | 选中项目后，树中项目没有视觉高亮，用户无法判断当前选中的项目。 |
| **根因** | `set_data()` 中 `self.clear()` 重建树后，仅用 `setSelected(True)` 不足以解决 Qt 的视觉高亮。 |
| **修复** | `set_data()` 新增第三阶段：在树重建后遍历所有项目节点，找到选中项目后调用 `setCurrentItem()` 确保视觉高亮和焦点持续。 |
| **涉及文件** | `src/ui/widgets/theme_tree.py`（`set_data`）。 |

#### ✅ 点击项目不再强制跳转数据设置页

| 项 | 说明 |
| :--- | :--- |
| **问题现象** | 用户在图表预览页或系列配置页查看时，点击项目树中的项目，页面被强制跳转到数据设置页，打断当前操作。 |
| **根因** | `_on_project_selected()` 末尾有 `self.tabs.setCurrentWidget(self.import_view)` 强制切换标签页。 |
| **修复** | 移除该行。用户点击项目后，仅更新项目数据和 ImportView 控件状态，保留当前所在页面不动。文件缺失和新建项目场景的跳转保持不变（合理引导）。 |
| **涉及文件** | `src/ui/main_window.py`（`_on_project_selected`）。 |

#### ✅ 窗口最小尺寸修复（消除 QWindowsWindow 警告）

| 项 | 说明 |
| :--- | :--- |
| **问题现象** | 启动时控制台输出 `QWindowsWindow::setGeometry: Unable to set geometry` 警告，窗口最小尺寸限制（2227px）超过屏幕宽度（1920px）。 |
| **根因** | MainWindow 未设置 `minimumSize`，Qt 从子控件（splitter 三栏 + ECharts 预览）的最小尺寸 hint 自动推算，累加值超过屏幕物理尺寸。 |
| **修复** | `MainWindow.__init__` 中新增 `self.setMinimumSize(1024, 600)`，覆盖子控件的最小尺寸推算值。 |
| **涉及文件** | `src/ui/main_window.py`（`__init__`）。 |

#### ✅ 系列配置双行布局（小屏幕适配）

| 项 | 说明 |
| :--- | :--- |
| **问题现象** | 小屏幕桌面尺寸下，系列配置页每行 15+ 个控件拥挤在单行 HBoxLayout，字体重叠无法操作。 |
| **根因** | `SeriesRowWidget` 使用单一 `QHBoxLayout`，未考虑窄屏适配。 |
| **修复** | 改为双行 QVBoxLayout 布局：第一行 = 显示/名称/X列/Y列；第二行 = 起始行/结束行/颜色/形状/大小。每行控件数量从 15 降至 7~8 个，小屏幕不再重叠。 |
| **涉及文件** | `src/ui/views/config_view.py`（`SeriesRowWidget.__init__`）。 |

#### ✅ 面板宽度可自由调节

| 项 | 说明 |
| :--- | :--- |
| **问题现象** | 项目组树和图表列表面板宽度固定，用户无法根据需要调整。 |
| **修复** | Splitter 新增 `setStretchFactor`（左0/中0/右1）使右侧面板自动伸缩；左面板最小 160px、中面板最小 140px，用户可拖拽分隔条自由调整三栏宽度。 |
| **涉及文件** | `src/ui/main_window.py`（`_build_ui` splitter 配置）。 |

#### ✅ 点击项目组不再强制跳转项目

| 项 | 说明 |
| :--- | :--- |
| **问题现象** | 鼠标点击项目组节点时，会强制跳转到当前已选中的项目，项目组节点无法正确高亮。 |
| **根因** | `_on_group_selected()` 调用 `_refresh_tree()` 时传入了 `self.current_project`，导致 `set_data()` 中 `setCurrentItem(pitem2)` 将当前项目设为"焦点项"，覆盖了组的选中状态。 |
| **修复** | `_on_group_selected()` 改为直接调用 `tree.set_data()` 并传入 `None` 作为 `sel_project`，避免树重建时 `setCurrentItem` 强制跳到项目，组节点正确高亮。 |
| **涉及文件** | `src/ui/main_window.py`（`_on_group_selected`）。 |

#### ✅ 项目组左键点击高亮修复

| 项 | 说明 |
| :--- | :--- |
| **问题现象** | 右键点击项目组可以高亮，但左键点击无法保持高亮，用户无法识别当前选中的项目组。 |
| **根因** | `set_data()` 第三阶段仅对 `sel_project` 调用 `setCurrentItem()`，没有对 `sel_group` 做同样处理。Qt 中 `setSelected(True)` 仅标记选中状态，视觉高亮（`:selected` 样式）依赖 `setCurrentItem` 设置的"当前项"。右键能高亮是因为 Qt 的 `contextMenuEvent` 默认行为会先调用 `setCurrentItem`。 |
| **修复** | `set_data()` 第三阶段新增 `sel_group` 分支：遍历组节点找到匹配项后调用 `self.setCurrentItem(gitem3)`，确保左键和右键点击项目组都能正确高亮。 |
| **涉及文件** | `src/ui/widgets/theme_tree.py`（`set_data` 第三阶段）。 |

#### ✅ 项目树三层高亮重构（v3.3.1）

| 项 | 说明 |
| :--- | :--- |
| **问题现象** | 用户点击图表时高亮跳回项目组，点击项目时高亮跳回项目组，无法准确定位当前选中的层级。 |
| **根因** | set_data() 第三阶段用 if sel_group is not None: ... elif sel_project is not None: 的 if/elif 链，而 sel_group 永远有值（current_group 始终保留），导致永远只走第一个分支，图表从未被 setCurrentItem 。 |
| **修复** | 第三阶段完全重写，反转优先级为图表 > 项目 > 项目组，用 ound 标志量确保用户点击哪个层级就高亮哪个，不会跳回上层级。 |
| **涉及文件** | src/ui/widgets/theme_tree.py（set_data 第三阶段）、src/ui/main_window.py（_on_chart_selected 移除强制跳转）。 |

#### ✅ 点击项目不再强制跳转数据设置页

| 项 | 说明 |
| :--- | :--- |
| **问题现象** | 用户在图表预览页或系列配置页查看时，点击项目树中的项目，页面被强制跳转到数据设置页，打断当前操作。 |
| **根因** | _on_chart_selected() 末尾有 	abs.setCurrentWidget(self.preview) 强制跳转到图表预览。 |
| **修复** | 移除 _on_chart_selected() 中的 self.tabs.setCurrentWidget(self.preview) 调用，用户点击项目后保留当前所在页面。 |
| **涉及文件** | src/ui/main_window.py（_on_chart_selected）。 |

#### ✅ 点击项目组不再强制跳转项目

| 项 | 说明 |
| :--- | :--- |
| **问题现象** | 鼠标点击项目组节点时，会强制跳转到当前已选中的项目，项目组节点无法正确高亮。 |
| **根因** | _on_group_selected() 调用 _refresh_tree() 时传入了 self.current_project，导致 set_data() 中 setCurrentItem(pitem2) 将当前项目设为“焦点项”。 |
| **修复** | _on_group_selected() 改为直接调用 	ree.set_data() 并传入 sel_project=None，避免树重建时 setCurrentItem 强制跳到项目。 |
| **涉及文件** | src/ui/main_window.py（_on_group_selected）。 |

#### ✅ 图表预览标签页解锁

| 项 | 说明 |
| :--- | :--- |
| **问题现象** | 配置系列后切换到“图表预览”标签页，图表不显示，空白。 |
| **根因** | 标签页切换无任何信号触发 _preview()。预览仅在加载图表、修改系列、修改选项时触发，不包括手动切换标签页。 |
| **修复** | 	abs.currentChanged 信号连接 _preview()，切换到任意标签页时自动触发渲染。 |
| **涉及文件** | src/ui/main_window.py（_build_ui）。 |
